/**
 * URLs to pre-render to static HTML at build time (full SSG for all public app routes).
 * Still Vite + React — not Next.js.
 */
export const PRERENDER_PATHS = [
  '/',
  '/parents',
  '/schools',
  '/videos',
  '/privacy',
  '/privacy-policy',
  '/checkout',
  '/checkout/register',
]
